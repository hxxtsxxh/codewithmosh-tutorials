# The input command will receive input from the user store that data for further evaluation.
# We assign this input statement to any variable that we want. For example, here we give a variable called "name".
name = input('What is your name? ')
fav_color = input('What is your favorite color? ')
print(name + ' likes ' + fav_color)
